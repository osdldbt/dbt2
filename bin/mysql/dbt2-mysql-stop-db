#!/bin/bash

#
# This file is released under the terms of the Artistic License.
# Please see the file LICENSE, included in this package, for details.
#
# Copyright (C) 2006      Open Source Development Labs, Inc.
#               2006-2010 Mark Wong
#

if [ "x" = "x${MYSQLDIR}" ]; then
	echo "ERROR: MYSQLDIR must be set"
	exit 1
fi

HOSTNAME=`hostname`

if [ ! -f "${MYSQLDIR}/${HOSTNAME}.pid" ]; then
	echo "MySQL pid file '${MYSQLDIR}/${HOSTNAME}.pid' does not exist."
	echo "MySQL was not stopped, if it was running."
	exit 0
fi
MYSQL_PID=`cat ${MYSQLDIR}/${HOSTNAME}.pid`
kill ${MYSQL_PID} || exit 1
sleep 10
echo "MySQL stopped successfully (by kill)."

exit 0
