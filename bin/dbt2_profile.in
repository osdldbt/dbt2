#!/bin/sh

#
# This file is released under the terms of the Artistic License.
# Please see # the file LICENSE, included in this package, for details.
#
# Copyright (C) 2006 Mark Wong & Open Source Development Lab, Inc.
#

DIR=`dirname $0`

# Database instance name.
DBNAME="dbt2"

# Path of database data.
DBDATA=${HOME}/local/dbt2/data; export DBDATA


if [ -f "${HOME}/.${DATABASE}_profile" ]; then
	source ${HOME}/.${DATABASE}_profile
elif [ -f "@abs_top_srcdir@/bin/${DATABASE}/${DATABASE}_profile" ]; then
	source @abs_top_srcdir@/bin/${DATABASE}/${DATABASE}_profile
else
	echo "Cannot source '${HOME}/.${DATABASE}_profile' or"
	echo "'@abs_top_srcdir@/bin/${DATABASE}/${DATABASE}_profile'."
	exit 1
fi
